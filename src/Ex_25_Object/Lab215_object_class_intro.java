package Ex_25_Object;

public class Lab215_object_class_intro {
    // object class is the father/super class of every class.
    /*
    object class inculde:
    to String() method
    hashCode()
    equals()
    finalize()
    getClass()
    Clone()

    wait()method: causes the current thread to wait until thread invokes notify() or notifyAll() on this object.
    notify()method: Wakesup a single thread waiting on this objects monitor.
    notifyAll()method: wakesup all threads waitings on this objects monitor.

     */
}
