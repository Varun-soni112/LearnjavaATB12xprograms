package Ex_16_Arrays;

public class Lab130_Array_Introduction {
    //Array is a data Structure which have a fixed length of  same size.
    /*
    Array include two  ways of declaration
    1st:- Declaration
    int[] numbers;

    //Initialization
    number=new int[2];

    2nd:- Declarartion
    String[] browser={ "chrome", "firefox", "Edge"};
     */

    /*
    Iterate through Array in two ways.
    1st way:-by using for loop
    for(int i=0; i<browser.length; i++)
    {
    System.out.println(browser[i]);
    }
    2nd way:- by using for each loop
    for(String browser: browsers)
    {
     System.out.println(browser);
    }
     */

    /*
    Multidimensional Arrays: it include rows and coloum .
    String[][] credentials ={
       { "adult", "adimin123"}
       { "user" , "user123" }
                     };
                     sout(credentials[0][0]);  output: admin.


    }

     */
}
